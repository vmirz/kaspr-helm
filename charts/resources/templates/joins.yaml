{{- if .Values.joins }}
{{- $root := . -}}
{{- range .Values.joins }}
---
apiVersion: kaspr.io/v1alpha1
kind: KasprJoin
metadata:
  name: {{ .name }}
spec:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
